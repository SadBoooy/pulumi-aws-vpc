name: pulumi-aws-vpc
description: Provision AWS VPC using Pulumi (Python)
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
